package mango.eventHandlers;

public class WindowResizeListener {

    public static void resizeCallack(long glfWindow, int width, int height){
//        Window.get().setWidth(width);
//        Window.get().setHeight(height);
//
//
//        // get largest are that fits aspect ratio
//        int aspectWidth = width;
//        int aspectHeight = (int)((float)aspectWidth / Window.get().getTargetAspectRatio());
//        if(aspectHeight > height){
//            // switch to pillarbox
//            aspectHeight = height;
//            aspectWidth = (int)((float) aspectHeight * Window.get().getTargetAspectRatio());
//        }
//
//
//        // center the window
//        int viewPortX = (int) (((float)width / 2f) - ((float)aspectWidth / 2f));
//        int viewPortY = (int) (((float)height / 2f) - ((float)aspectHeight / 2f));
//
//        glViewport(viewPortX, viewPortY, aspectWidth, aspectHeight);
    }


}
